<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dreamreco.joodiary">

    <!--    WRITE 권한에 포함-->
    <!--    tools:ignore="ScopedStorage"-->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- requestLegacyExternalStorage 영구 개선 필요 -->
<!--    android:requestLegacyExternalStorage="true"-->
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/joo_diary_text_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/joo_diary_text_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JooDiary"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.BioScreenLockActivity"
            android:theme="@style/SplashTheme"/>
        <activity android:name=".ui.login.PasswordScreenLockActivity"
            android:theme="@style/SplashTheme"/>
        <receiver android:name=".ui.login.ScreenReceiverWithBio"/>
        <receiver android:name=".ui.login.ScreenReceiverWithPassword"/>
        <service android:name=".ui.login.ScreenService"/>
    </application>

</manifest>